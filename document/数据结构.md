{
tabs: [
{
id: string,                    // uuid，唯一标识
index: number,                 // 顺序（可选，通常用于拖拽排序）
title: string,                 // 网站标题
url: string,                   // 网页地址
icon: string,                  // 图标url或特殊值（如 'loading'、'default'）
loading: boolean,              // 是否加载中
active: boolean,               // 是否激活
pinned: boolean,               // 是否固定
groupId: string,               // 分组ID（可选）
type: 'normal' | 'sync' | 'native', // 标签类型
muted: boolean                 // 是否静音
}
],
activeTabId: string,               // 当前激活标签的id
lastActiveTabId: string            // 上一个激活标签的id
}